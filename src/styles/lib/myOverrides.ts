import { NInput } from 'naive-ui'

type NInputThemeOverrides = NonNullable<NInput['themeOverrides']>

export const nInputThemeOverrides: NInputThemeOverrides = {
  textColor: 'black',
  borderColor: 'black',
  placeholderColor: '#979793',
  hoverColor: 'black'
}